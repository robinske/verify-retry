{
  "name": "verify-retry",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "twilio": "^3.66.0"
  },
  "devDependencies": {
    "twilio-run": "^3.1.0"
  },
  "engines": {
    "node": "12"
  }
}
